# Runtime dependencies shared by every environment.
Django>=5.2.17,<5.3          # LTS, supported until April 2028
django-environ==0.14.0       # settings from .env / environment variables
whitenoise==6.12.0           # serves static files (CSS/JS) without a separate web server
mysqlclient==2.3.0           # MySQL driver recommended by Django
# Fernet: encrypts Zoom host keys at rest (brief 005, D17)
cryptography==50.0.1         # ships manylinux wheels, so no build tools are needed in any image stage
# HTTP client for the Zoom API (brief 006, D3)
requests==2.34.2             # pure Python; verifies TLS by default, per-call timeouts, pooled connections
