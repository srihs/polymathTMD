# prod.txt, not base.txt: the dev image then holds gunicorn, so apps/core tests can load
# docker/gunicorn.conf.py (the access-log token filter, brief 011). One pin, in prod.txt.
-r prod.txt

pytest==9.1.1
pytest-django==4.14.0
ruff==0.16.7
# mocks Zoom at the HTTP layer in tests (brief 006, D3)
responses==0.26.3            # conftest.py turns any unregistered requests call into an error
