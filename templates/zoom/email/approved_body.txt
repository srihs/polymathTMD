{# Body of the "link approved" message, to the verified requester only (brief 005, D.8; brief 011, D.8.1 and criterion 28). Plain text. The host key is never emailed any more: with a start link (the zoom and fake providers) the email carries the "Starting the class as host" block, the "share the join link, keep the start link" line (P7) and the don't-forward sentence; with start_url "" (the manual provider, G5, P3) it says to ask the IT desk and never mentions a start link. Context: link_request (LinkRequest with requester_name, reference, join_url, meeting_id, passcode, wants_recording, schedule_summary), occurrences (list of Occurrence), start_url (str, absolute zoom:start URL, or "" for the manual provider). #}{% autoescape off %}Hello {{ link_request.requester_name }},

The IT desk has approved your request {{ link_request.reference }}. Here is your Zoom link.

Join link: {{ link_request.join_url }}
Meeting ID: {{ link_request.meeting_id }}
Passcode: {{ link_request.passcode|default:"none" }}

Use the same link for every class below.{% if start_url %} Share the join link with your students. Keep the start link to yourself.{% endif %}

{% if start_url %}Starting the class as host
Start link: {{ start_url }}
Open this link from 30 minutes before each class until it ends, then press Start this class. It starts the meeting as host, with no Zoom sign-in. Keep it private: anyone who has it can start these classes as host.{% else %}Ask the IT desk how to start the class as host.{% endif %}

{% if link_request.wants_recording %}You asked for this class to be recorded to the Zoom cloud.

{% endif %}When: {{ link_request.schedule_summary }}
Your classes ({{ occurrences|length }}):
{% for occurrence in occurrences %}- {{ occurrence }}
{% endfor %}
To change the times, reply to the IT desk.{% if start_url %} Please don't forward this email, because the start link in it lets anyone start your classes as host.{% endif %}

Polymath College IT desk
{% endautoescape %}
